apiVersion: v1
kind: Service
metadata:
  name: mosquitto-service
  namespace: fleet-tracker-staging
  labels:
    app: mosquitto
spec:
  selector:
    app: mosquitto
  ports:
    - name: mqtt
      protocol: TCP
      port: 1883
      targetPort: 1883
  type: ClusterIP

